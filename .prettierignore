node_modules/
dist/
build/
coverage/
pnpm-lock.yaml
.claude/

# Generated artifact — `pnpm --filter @getmonoceros/workbench catalog:json`
# owns its format (JSON.stringify 2-space). Keep prettier from fighting it.
catalog.json

# Hand-authored skill content (SKILL.md). It is prose/instructions read by
# Claude, and its bytes must match the published zip; don't let prettier reflow
# it (that would force a needless re-zip + web redeploy on every format pass).
skills/

# Prompt content, read by an AI agent and copied by it verbatim (the plan
# template inside monoceros-planner.md is a literal). Same reasoning as
# skills/ above: prettier reflowing a table or a fence changes what the model
# reproduces, for no benefit to a human reader.
packages/cli/templates/opencode-roles/
